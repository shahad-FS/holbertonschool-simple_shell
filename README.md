# simple_shell

A simple UNIX command line interpreter implemented in C.

## 📖 Description

This project is a simple shell that can execute commands, handle PATH, and includes built-in commands such as `env` and `exit`.
It supports interactive mode and basic parsing of arguments.

## ✨ Features

- 🖥️ Execute commands with absolute or relative paths.
- 🔍 Search for commands in the PATH environment variable.
- ⚙️ Built-in commands:

  - `env`: print all environment variables
  - `exit`: exit the shell
- 🧹 Handle extra spaces and tabs in the input.
- 💻 Interactive prompt display.


---

### Authors:
Shahad Alsaneea.

Rahaf Alhareti.
